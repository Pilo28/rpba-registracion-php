RewriteEngine On

# Servir archivos y directorios reales tal cual (assets, api/asistente.php)
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Todo lo demás pasa por el front controller
RewriteRule ^ index.php [L]
